# Changelog

Generated by `./build changelog` from the commit history. Never edited by hand: an entry here
is a conventional commit subject, so fixing the wording means amending the commit.

## Unreleased

### Features

- **settings**: player graphics settings for msaa, fxaa, shadows, post passes, field of view and render scale
- **renderer**: camera motion blur from the normal buffer's distance and the last frame's camera
- **render2d**: haze veils between parallax planes, the 2D match for fog
- **renderer**: aerial perspective turns distant surfaces toward the fog's hue at their own brightness
- **renderer**: light shafts gathered toward the sun at half resolution
- **renderer**: eye adaptation measured on the GPU into a one texel history, exposing mid tones and saturation
- **debug**: a trace page in the overlay, and gnyame keys to page, sort, log and capture
- **trace**: scope shadows, the scene, post passes, physics, audio, net and assets, and time gpu groups through fences
- **debug**: trace time, calls, draws and memory per feature, with a chrome trace capture
- **net**: persistent key pairs, --server-key and --net-* conditioner flags, per peer stats in the debug overlay, and gnyame's server holding players to its speed
- **net**: draw replicas on a timeline with a delay that follows jitter, capped extrapolation, and clients that tick at the server's rate
- **net**: delta snapshots in fixed point against the acknowledged baseline, compact commands carrying the ack, and server authority over command rate, tick and speed
- **net**: encrypt and authenticate every udp packet, with a stateless x25519 handshake, replay window, handshake rate limit, per address connection cap and a network conditioner
- **packaging**: ship the Steam depots and verify the Linux one against sniper
- **build**: steam-linux and steam-windows targets
- **steam**: relaunch through Steam, connect, pump callbacks and disconnect from nya_app
- **integrity**: sweep the code, check blob entries and watch the checks run
- **audio**: trace sound through the world and run an effect chain on every bus
- **gnyame**: flat menus and HUDs on the new UI, a look panel with every widget, and a saved player name
- **ui**: containers with fixed, fit and grow sizes, a window scale, pushed styles, nine-slice skins, transitions, text input and a colour picker
- **settings**: keep the player's name, and tell a given launch name from the default
- **render2d**: cut nine-slices from sheet regions, tile or scale them, and draw gradient rects
- **skeleton**: sample the pose between the clock's last tick and its current time for drawing
- **entity**: draw entities between the previous tick and the current one
- **gnyame**: a flat coloured look by default, no ink, soft occlusion, gentle grade and scuff marks instead of paint and stars
- **renderer**: occlusion softness, and shading that fades from bands to a smooth gradient past half roughness
- **renderer**: converge speed lines on where the camera heads
- **renderer**: give ambient occlusion a minimum reach in pixels
- **renderer**: allocate the shadow atlas only while a scene casts shadows
- **debug**: show the renderer's draws, passes and upload bytes per frame in the overlay
- **renderer**: record the 3D scene once and draw every shadow cascade from the same upload
- **gnyame**: menus and HUD panels on the ui module, with stats and language options in the pause menu
- **ui**: panels grow past their size to fit content, and take a fill colour
- **ui**: immediate-mode panels, rows, buttons, selectables, toggles and sliders
- **renderer**: rounded rectangles and rounded outlines in the 2d batch
- **gnyame**: depth of field, speed lines, decals and hdr in the 3d demo, fed from the config
- **renderer**: hdr output where the display supports it
- **renderer**: projected box decals drawn in one batched pass
- **renderer**: cartoon speed lines after the caller's post passes
- **renderer**: toy depth of field in the post chain, tilt shift or focused on a distance
- **gnyame**: feed the shade colour from the config and tint the ambient by the sky
- **renderer**: colour the shade and the ambient for a cartoon look
- **gnyame**: grade the 2D world and the 3D scene from the config
- **renderer**: grade through a 3D colour lookup table
- **renderer**: make shadow cascades and map size per window options
- **core**: saves and logs live under the app id in the user data directory
- **gnyame**: cartoon passes in the 3D demo, fed from the config
- **renderer**: scene normal buffer and cartoon post passes
- **gnyame**: log what the drones trained when they are turned off
- **gnyame**: drones that learn to fly to the player, NEAT and DQN trained on a job, a nav flow field around the terrain, the best genome saved and every run kept in sqlite
- **gnyame**: gamepad bindings for walking and the menus, polled by the menu widget, with a virtual pad test
- **ci**: publish a windows zip, the package manifests and checksums on tag
- **packaging**: add steampipe build scripts and an upload script
- **packaging**: add a flatpak manifest wrapping the linux release archive
- **packaging**: add winget and scoop manifests for the portable windows zip
- **packaging**: add an aur -bin package built from the release archive
- **build**: link a version resource into the windows release executable
- **gnyame**: msaa_samples in the config, a 2D scene target without depth, and the world greyed behind the pause menu
- **renderer**: runtime MSAA through NYA_RenderOptions, single sampled render textures, and a post chain that holds a second target only while passes need it
- **gnyame**: log every arena with its resident bytes when the overlay opens
- **debug**: show resident bytes per arena and for the process in the overlay
- **base**: measure how much of each arena is resident
- **platform**: reserve, commit and measure residence of virtual memory
- **gnyame**: draw the menus through registered fonts, with a distance field title
- **renderer**: cache laid out text keyed on face, size, text and wrap width
- **gnyame**: set the demo animation speed from the config file
- **gnyame**: animate the moving ledge's marker with the sprite animator
- **gnyame**: draw the skinned bender bar in the 3D demo
- **base**: add NYA_Cache, a fixed capacity cache keyed on content
- **build**: run parallel rules in a pool that refills as each finishes
- **renderer**: count GPU memory by kind and show it in the debug overlay
- **packaging**: ship a Linux desktop entry, icon and installer, and set the app id
- a lot. cba to split this
- ring
- job system
- event oneshot hooks
- stack allocation options for ds
- several improvements
- function pointers -> callback system
- shader support in asset pipeline
- asset system v3
- event hooks
- look at j*b system
- asset loading and hot-reloading
- asset manifest
- stub j*b system
- event system
- system refactor
- intern nya app struct
- triangle!
- shader compiling
- linking with steam
- add steam vendor dependency
- cycle counting in perf module
- allocation tracking
- set operation macros for hset
- hashmap and hashset
- hashing
- nya_assert_panic long jumps out of the code
- submodule update command
- test filtering
- code hot reloading
- argument parsing #2
- type names
- arg parsing
- little type refactor
- add f128
- panic prevention
- lexer
- accept hex seed for rng
- sampling from distributions
- random number generator
- perf metrics
- test system
- build system
- moving towards a build system
- app and windows
- strings
- array
- arena allocator
- memory macros
- vector and matrix types
- logging and assertions
- compiler attributes
- bits
- integer types
- project setup

### Bug Fixes

- **build**: print the failing rule, not the usage block
- **test**: the replica map on the arena, not the stack
- **build**: monocypher in the steam runtime vendor set
- **clangd**: the plugin, vendor and lint lists the editor was missing
- **test**: wait for the clock rather than trusting Sleep in the perf timer test
- **build**: room for twenty four vendors per rule, now that windows links monocypher beside steam
- **net**: tolerate the fixed point step when reconciling, and fit the overlay line in its column
- **integrity**: sweep from the update, which a live resize keeps running
- **platform**: define the mmap flags glibc 2.31 hides under strict POSIX
- **generated**: regenerate the reflection header after merging audio and ui types
- **gnyame**: hear the fire from the middle of its flames
- **audio**: judge a way around a blocker by how far it detours
- **render3d**: sort a transparent quad's two triangles as one, so overlapping billboards stop interleaving half by half
- **gnyame**: no edge darkening on the models, which drew a dark outline around them
- **gnyame**: draw networked players between ticks
- **gnyame**: draw crates, ledges, drones, the 3D props, the bar's pose and the 2D camera between ticks
- **entity**: leave a 3D body's transform to the solver instead of integrating its mirrored velocity again
- **gnyame**: scuffs use the soft round decal cell
- **generated**: count the bloom options among the reflected types
- **text**: measure a distance field as wide as the same face in coverage
- **integrity**: hash only the executable segment from the program headers
- **renderer**: keep the HUD at paper white in HDR
- **shader**: predict ink silhouettes from the distance slope and require creases to hold past slivers
- **particles**: draw between the previous tick and the current one, so particles stop stuttering when frames and ticks do not line up
- **build**: mark mingw's __cpuidex defined for every windows compile, c included, so clang's is the only one
- **build**: keep older mingw-w64 headers from defining __cpuidex for clang's c++ windows vendor builds
- **renderer**: snap the shadow light basis to half degree steps
- **renderer**: filter shadow taps bilinearly and cut the edge crisp
- **input**: roll gamepad edges at the end of each update tick, as keys do
- **windows**: undefine windows.h's near and far macros once the engine is included
- **input**: four bindings per action, and a settings load keeps the gamepad bindings it has no keys for
- **test**: name frustum planes so windows' NEAR and FAR macros do not replace them
- **platform**: parse statm with strtoull, which reports what it could not convert
- **shader**: halve the distance field smoothing floor to one pixel at 1:1
- **platform**: quote windows command arguments the way the runtime splits them, and keep command environment out of the parent
- **renderer**: single sample the skinned shadow pipeline
- **assets**: load skinned vertices and root bones in bind time world space
- **render2d**: leave room for the distance field spread in glyph cells
- **ci**: drop duplicated if keys on the compiler cache save steps, run the pwd test in a directory Windows has
- **perf**: make the scope timer cleanup static inline, and link shared engine tests without multiple definitions
- **gnyame**: keep the 2D world in its offscreen target whether or not bloom is on
- **window**: show a window only once its pipelines are built, and prefer native Wayland
- **renderer**: pick compiled shaders by what the GPU device accepts, not by OS
- **net,arena**: free loopback messages once polled, and shrink default arena regions to 1 MiB
- **windows**: report missing files as NOT_FOUND, and read wall time precisely
- **build**: name host executables with the suffix the Windows linker adds
- **windows**: make the tests compile on an LLP64 target
- **hot-reload**: keep the game valid across a code reload
- **filesystem**: take file modes as flags, and assert copy has two open descriptors
- **config**: repoint a config watch when a game re-attaches after a code reload
- **assets**: free font and sound bytes on unload, share expanded blob entries
- **renderer**: key registered meshes by handle text, not by pointer
- **net**: keep duplicate suppression intact under reordering, bound fragmented reliable messages
- **animation**: direction-aware skeleton events, root motion in the character's frame
- **assets**: order FBX bones parent first and bind every skinned mesh
- **main**: reload the game DLL once it stops changing
- **platform**: spawn children with default SIGPIPE and close-on-exec pipes
- **renderer**: do not occlusion-cull shadow casters
- **renderer**: register the unit sphere past the reserved-handle check
- **renderer**: store the shadow depth across a pass suspend
- **renderer**: restore the cascade viewport after every shadow flush
- **renderer**: shadow passes after cascade zero drew nothing
- **renderer**: release the shadow pass depth vertex buffers
- make icon.ico a lfs object
- typo
- add back perf test
- tests
- move dynamic datastructures to the heap
- update graph
- perf function macro
- strcmp -> nya_string_equals
- formatting
- rename embedding
- per system allocators
- bug fixes and tests
- some cleanup
- disable steam for now
- build system error checking
- spacing
- test linker error
- update attribute macros
- remove asan and optimization from build for x4 speedup in compiling it
- add clanker wanking disclaimer to tests
- keeping a pointer to a stack variable
- changed overlooked when refactoring earlier
- fix some bugs
- wrong exit code
- typo
- clamp random variables instead of crashing if out of bounds
- matrix function naming
- missing avx target feature
- static -> nya_internal and extern -> nya_external
- more format arguments for assert
- namespace matrix multiplication

### Performance

- **render2d**: merge sorted ranges with the same state into one draw call
- **font**: skip the distance field request scan until an asset loads
- **renderer**: bloom at half resolution as a built-in post pass
- **ui**: keep the ui tables out of the binary and draw each widget's text after its shapes
- **assets**: bake only the shader formats a target can load, and leave the unused icon set out
- **audio**: allocate a bus's reverb lines the first time its room is switched on
- **net**: allocate replica maps and peer state when a connection needs them
- **core**: commit the entity table as spawning reaches it
- **core**: stop the tween target scan at the last live slot
- **renderer**: spell font handles without snprintf
- **assets**: narrow NYA_VertexSkinned3D from 96 to 44 bytes
- **render2d**: hold 128 glyphs per atlas and warn once when one fills
- **render2d**: stage glyph uploads through a cell sized transfer buffer
- **startup**: first frame in about 65 ms instead of 135
- **assets**: only store an entry compressed when it actually saves
- **assets**: compress the asset blob, and encode the music as Opus
- **renderer**: store glyph atlases as R8 instead of RGBA8
- **renderer**: let a render texture skip its depth buffer
- **renderer**: store the shadow map as R16_UNORM
- **renderer**: lay the shadow atlas out as a strip

### Tuning

- **gnyame**: run at 30 fps instead of 15 while the window is unfocused

### Refactors

- **build**: fold the one declaration headers into the files that own them
- **ui**: drop nya_ui_last_rect, which only the tests read, and probe layout through the pointer and nya_ui_space
- **renderer**: remove the inverted hull outline
- **render2d**: move the glyph atlas grid and coverage bake into render_glyph_atlas.c
- **renderer**: move frustum culling into render_cull.c so headless tests reach it
- **core**: move the asset memo, glyph atlases and mesh registry onto NYA_Cache
- **gnyame**: read player speed and spawn spacing from the config, and start music from a system
- **gnyame**: shrink the HUDs to a few translated lines and the engine's debug overlay
- **gnyame**: split the layers header by domain, and document how the game uses the engine

### Documentation

- **readme**: not every ci package is needed to build
- **todo**: trace merged, renderer branch still to rebase
- **todo**: list the unmerged renderer and tracing branches
- **todo**: record networking security, steam targets and anti-tamper
- **todo**: record raytraced audio, bus effects and the finished ui
- **todo**: the definition of done for the engine
- **todo**: entities, camera and poses interpolate between ticks
- **todo**: the renderer aims for a flat coloured look, not a cartoon
- **todo**: record ink, occlusion, bloom, hdr, speed line and sdf polish
- **todo**: tick interpolation
- **todo**: record the once per frame scene upload
- **todo**: record the ui module
- **todo**: record the depth of field, decals, hdr and speed lines, and the binary size work
- **todo**: ci is green
- **todo**: record grading, shade colours and shadow work
- **todo**: binary size
- **todo**: gamepad edges roll per tick
- **todo**: saves and logs follow the app id
- **todo**: record the screen-space cartoon passes
- **todo**: record the learning drones, gamepad menus and what stays unwired
- **todo**: record packaging and what steam still needs
- **packaging**: describe how to cut a release for each channel
- **todo**: record the text cache, skinned bar, culling and glyph tests, runtime msaa and memory work
- **renderer**: size the text run cache comment from counted strings
- **todo**: track the cartoon renderer work and the ci workflow fix
- **todo**: record the reports from other machines
- **todo**: record startup timings and the remaining gnyame cleanup
- **todo**: record the reload finding and the Windows shader constraint
- drop filler intensifiers from comments
- drop the last planning references and marked comments from the benches and game config
- **todo**: tighten, and record the CI layout, the LuaJIT finding and the config reload gap
- **tests**: cut test comments down to what each case pins
- cut the last marked comments down to the reasons
- cut comments down to the reasons in the build recipes, serde, fonts, plugins, platform, controller, net and container macros
- cut comments down to the reasons in net.h, quaternion, world, skeleton, sim, save, nav, ceilings, reflection, integrity, hset, bench, args, arena and the game sim
- cut comments down to the reasons in assert, game headers, ufbx, sdl, shadows, occlusion, curl, net headers and the section banners
- cut comments down to the reasons in text, lod, headless 3D, neat, shapes, matrix, window, tween, tilemap, skeleton, events, integrity and backtrace
- cut comments down to the reasons in physics3d.h, dqn, snapshots, net_client.h, overlay, inertializer, entity, config, diagnostics, shadercross and the game entry
- cut comments down to the reasons in camera, headless 2D, font, matrix, lua bindings and the build tool
- cut comments down to the reasons in core_app.h, sprites, nn_tensor, i18n, base_compress.h and the game layer
- cut comments down to the reasons in physics3d, net_client, debug_overlay, entity_box and render_text
- cut comments down to the reasons in glass, nn_neat_draw, core_save.h, particles and discord
- **lua,input**: cut comments down to the reasons, and write the missing lua.h notes
- cut comments down to the reasons in terrain3d, uniforms.h, layers.c, base_arena.c and core_tilemap.c
- cut comments down to the reasons in physics2d, core_app, core_audio.h, the cube3d layer, constants and the mesh shading include
- **nn,net,core,renderer**: cut comments down to the reasons
- **renderer,core,net**: cut comments down to the reasons
- **todo**: record what landed and what the review found
- **todo**: bring the list up to date with what has landed
- give the reason a line is what it is, not its edit history
- asset docs
- update todo list
- typos format explanation
- write down todos

### Tests

- **net**: fuzz every wire decoder from a fixed seed, and run a server and client through a bad network to convergence
- **audio**: filter responses, limiter ceiling, echo timing and propagation against a fake scene
- **renderer**: check the window keeps its shadow fit and a draw can opt out of the cascades
- **gnyame**: accept any drone moving, since an evolved genome may hold one still
- **core**: expect two cascades and the grade and shade fields in engine.nya
- **gnyame**: train the drones until a genome scores, since the game seeds evolution randomly
- **gnyame**: cover the menu widget, screen changes and config fallbacks headless
- **command**: find sh on PATH, which is where MSYS2 puts it on Windows
- **perf**: check overlapping timers by timestamp order, not sleep lengths
- **i18n**: rewrite the reload fixture until its timestamp moves
- add more coverage
- perf
- dll
- more tests
- string
- array
- arena
- lexer
- random
- file and filesystem
- clock
- math
- base_bits

### Benchmarks

- **net**: measure replication bytes per second for a crates and drones scene
- **audio**: price each effect unit and a frame of propagation against Box3D
- **entity**: price the per tick transform capture and the render transform read
- **core**: price the entity table's bring-up, spawning and per tick walk

### Build System

- **vendor**: add monocypher for the network handshake and packet encryption
- compile with f16c so half float vertex channels convert inline
- leave local symbols and the COFF symbol table out of release binaries
- cross compile windows vendors with clang, whose function sections a PE link can collect
- build vendors at -O2 with a section per function, and collect the unused ones in the release link
- export the engine API from windows executables only where a game DLL links against it
- build libbacktrace without debug info
- turn off the curl features the request plugin never asks for
- decode Vorbis, FLAC and MP3 with SDL_mixer's built in decoders, drop tracker, chip, WavPack and MIDI
- build SDL_ttf without plutosvg colour emoji
- decode images with the stb_image SDL_image carries, and drop WebP and the old paint formats
- compile out the parts of SDL the engine never calls
- size box2d and box3d's static world arrays for eight worlds instead of 128
- **test**: share the engine object with tests that only name identifiers the headers declare
- keep the commit hash off compile commands so ccache direct mode hits across commits
- split compile and link so ccache can serve engine and test objects
- compile shaders on Linux only, and hand them to the Windows jobs
- **lua**: name the compiler for LuaJIT on a Windows host
- fix box2d on a Windows host and lz4 under the build tool's sanitizers
- bootstrap the build tool without a prebuilt lz4 archive
- make clang-tidy and the compiler pass with zero findings

### Continuous Integration

- build and verify the Steam targets, and publish both depots on tag
- build vendors once per platform before the jobs that need them
- save the vendor cache right after vendors build, and key it on the toolchains too
- fix the failures hidden behind the ones that failed first
- give sqlite a real tclsh on Windows, keep restored vendor builds current
